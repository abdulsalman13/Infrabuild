name: Hello Infra Runner

on:
  push:
     branches: [ main ]

jobs:
   hello:
       runs-on: [ infra ]
       steps:
        - name: Checkout Code
          uses: actions/checkout@v4
        - name: listing the repo
          run: ls -l
